---
name: Swippy
---

import { Playground, Props } from 'docz'
import Swippy from '../src'
import { LeftElement, MainElement, RightElement } from './dummy'

# Swippy

## Properties

<Props of={Swippy} />

## Full usage

<Playground>
  <Swippy
    mainElement={<MainElement />}
    renderLeftElement={(isActive, opacity) => <LeftElement isActive={isActive} opacity={opacity} />}
    renderRightElement={(isActive, opacity) => <RightElement isActive={isActive} opacity={opacity} />}
    leftAction={() => alert('left action triggered')}
    rightAction={() => alert('right action triggered')}
  />
</Playground>

## Left only

<Playground>
  <Swippy
    mainElement={<MainElement />}
    renderLeftElement={(isActive, opacity) => <LeftElement isActive={isActive} opacity={opacity} />}
    leftAction={() => alert('left action triggered')}
  />
</Playground>

## Right only

<Playground>
  <Swippy
    mainElement={<MainElement />}
    renderRightElement={(isActive, opacity) => <RightElement isActive={isActive} opacity={opacity} />}
    rightAction={() => alert('right action triggered')}
  />
</Playground>
